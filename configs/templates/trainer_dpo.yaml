trainer:
  name: dpo_default
  algorithm: dpo
  beta: 0.1
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 8
  num_train_epochs: 3
  learning_rate: 1e-5
  weight_decay: 0.0
  warmup_ratio: 0.05
  save_steps: 500
  logging_steps: 25
