trainer:
  name: sft_default
  algorithm: sft
  max_steps: null
  num_train_epochs: 3
  per_device_train_batch_size: 4
  gradient_accumulation_steps: 4
  learning_rate: 2e-4
  weight_decay: 0.01
  warmup_ratio: 0.03
  logging_steps: 10
  save_steps: 200
  evaluation_strategy: steps
  eval_steps: 200
